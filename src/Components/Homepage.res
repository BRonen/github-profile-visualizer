@react.component
let make = () => {
  <>
    <Title className="mb-3"> {"Github Profile Search"->React.string} </Title>
    <SubTitle> {"Just type the username of the user on the header input"->React.string} </SubTitle>
  </>
}
